# .env.example

# MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=household_finance

# JWT secret for signing tokens (use a strong random value in production)
JWT_SECRET=supersecretreplace

# Optional: path to CA certificate when using DB TLS
# DB_SSL_CA=/path/to/ca.pem

# NODE_ENV=development
